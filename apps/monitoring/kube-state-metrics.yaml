# kube-state-metrics — Kubernetes object metrics collector
# Reads the k8s API → exposes pod status, deployment replicas, resource limits/requests
# Alloy scrapes it via prometheus.io/scrape annotation → ships to Mimir
# Required for Kubernetes cluster dashboards in Grafana
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kube-state-metrics
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
    - chart: kube-state-metrics
      repoURL: https://prometheus-community.github.io/helm-charts
      targetRevision: 5.30.0
      helm:
        values: |
          podAnnotations:
            prometheus.io/scrape: "true"
            prometheus.io/port: "8080"
  destination:
    namespace: monitoring
    name: in-cluster
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
