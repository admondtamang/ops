# node-exporter — host-level metrics collector (runs as DaemonSet)
# Reads /proc and /sys on the node → exposes CPU, RAM, disk, network metrics
# Alloy scrapes it via prometheus.io/scrape annotation → ships to Mimir
# Required for Node Exporter Full dashboard in Grafana
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: node-exporter
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
    - chart: prometheus-node-exporter
      repoURL: https://prometheus-community.github.io/helm-charts
      targetRevision: 4.44.0
      helm:
        values: |
          podAnnotations:
            prometheus.io/scrape: "true"
            prometheus.io/port: "9100"
  destination:
    namespace: monitoring
    name: in-cluster
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
